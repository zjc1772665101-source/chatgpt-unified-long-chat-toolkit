# Changelog

## 1.0.8

- 修复完整问答数量已保留、但未挂载历史项长期显示为 `Prompt N` 的问题。
- 展开“问答”页签时，按最多 3 项一批调用 ChatGPT 原生目录，让旧提问短暂挂载并读取真实标题；每批结束后恢复原问题与滚动位置。
- 使用 `conversation-turn-N` 的确定性序号映射写入标题，避免部分 DOM 造成标题前移、错位或短暂出现后消失。
- 用户交互会中止后台补全并恢复补全前页面状态；单项失败最多重试两次，避免循环刷新。
- 已补全标题仅写入当前标签页的 `sessionStorage`，刷新后可复用，关闭标签页后清理；未调用 ChatGPT 私有会话接口。

## 1.0.7

- 修复 ChatGPT 重挂载 `main`、切换 URL 查询参数或短暂缩减 DOM 时，“问答”目录从完整列表退回到 1～2 项的问题。
- 同一会话内保留轻量问答标签快照；只有会话路径真正变化时才清空，不会因此恢复全部回答正文。
- 官方导航与页面 DOM 在 hydration 期间临时缩小时，目录总数保持单调不回退；完整缓存也不再被早期小目录覆盖。
- 官方问答按钮暂时缺失时，不再把当前仅挂载的 1～2 个尾部问答误判为完整会话并覆盖前两项标签。

## 1.0.6

- 取消“章节”目录最终最多 18 项的硬截断，当前回答内的正式 H1-H6 标题现在全部显示。
- `answerTocDerivedMaxItems` 只继续限制无标题或标题稀疏时自动生成的派生章节，避免长回答产生过密的猜测目录。
- 保持旧问答 DOM 按压力停放；完整目录本身只渲染轻量按钮，不会因此重新加载所有回答正文。
- 补齐 H5/H6 的目录层级缩进和跳转顶部间距，避免深层标题看起来层级倒置或被页面顶部遮挡。

## 1.0.5

- 修复“章节”只读取 H1/H2、找到少量 H2 后便停止降级识别的问题。
- 章节始终严格限定在当前回答内部，与跨轮次的“问答”目录保持分离。
- 默认完整读取 H1-H6，并在标题较少时按回答长度和分隔线数量补充当前回答内的派生小节。
- 加强回答开头、真正位于段首的加粗标题、Markdown 粗体残留、分隔线和多层包装段落识别。

## 1.0.4

- 提示词卡片支持自由拖拽排序，并持久化顺序。
- 置顶和编辑改为 Tabler Icons 图标按钮。
- 桌面端仅在卡片悬停或键盘聚焦时显示操作图标；触摸设备保持可见。
- 过长的提示词标题和分类自动省略，完整标题保留为悬停提示。
- 拖拽时显示插入位置指示线，并阻止拖拽结束后误触发提示词输入。

## 1.0.3

- 合并长对话性能、双层导航、自适应大纲、提示词、会话导出、字体与滚动功能。
- 修复问答和章节跳转与生成期防自动沉底之间的冲突。
- 为尚未加载的历史问答保留并显示序号。
- 修复提示词编辑弹窗在短视口中的滚动和交互。
- 点击提示词卡片主体直接写入当前可见的 ChatGPT ProseMirror 输入框。
- 卡片操作区仅保留置顶和编辑；删除入口移入编辑弹窗。
- 发布前移除本机路径、用户名和机器特定字体快照。
